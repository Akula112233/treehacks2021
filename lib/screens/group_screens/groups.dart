import 'package:flutter/material.dart';

class GroupScreen extends StatelessWidget {
  static String id = "group_screen";
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Groups Screen")),
    );
  }
}
